# 天弱 · 开发进度与交接说明

> 本文件是**迭代交接的唯一真相来源**。

## 一、项目基本信息

| 项 | 值 |
|---|---|
| 项目名 | 天弱（面向 25 岁以下年轻人的交友 App，对标 陌陌 / Soul / 火花 / Solo） |
| 根目录 | `/data/workspace/tianruo` |
| 技术栈 | Node.js 20 + Express 4 + ws(WebSocket) + multer；前端原生 H5（零构建） |
| 数据层 | JSON 文件持久化 `data/db.json`（`server/store.js`），可平滑替换为 MySQL/Mongo |
| 启动命令 | `cd /data/workspace/tianruo && node server/index.js` |
| 停止命令 | `pkill -f "node[ ]server/index.js"` |
| 默认端口 | 3000（`PORT=xxxx node server/index.js` 覆盖） |
| 演示账号 | 手机 `13800000000` 密码 `123456`；验证码登录任意 11 位手机号 + 验证码 `1234` |
| 管理后台 | `/admin.html`，密钥 `tianruo-admin-2024`（可用 `TIANRUO_ADMIN_TOKEN` 覆盖） |

## 二、目录结构

```
tianruo/
├── server/
│   ├── index.js        # 入口：静态托管 + 路由装配 + 表情包 SVG
│   ├── config.js       # 全部配置（端口/密钥/额度/路径，均可用环境变量覆盖）
│   ├── store.js        # ★ JSON 持久化（load 幂等）+ 查询助手 q
│   ├── utils.js        # 密码哈希、Token、距离、年龄、工具函数
│   ├── moderation.js   # ★ 内容安全：敏感词 + 变体归一化 + 联系方式检测
│   ├── avatar.js       # SVG 头像生成（零依赖，永不 404）
│   ├── seed.js         # 种子数据：33 个虚拟用户 + 动态 + 预置匹配
│   ├── ws.js           # WebSocket：连接管理、定向推送、在线广播
│   ├── upload.js       # 图片/语音上传（multer）
│   └── routes/
│       ├── auth.js     # 验证码 / 登录 / 注册 / me
│       ├── user.js     # 资料、附近、谁喜欢我、访客、搜索、屏蔽、举报
│       ├── match.js    # 推荐卡片、滑动、匹配、解除、额度
│       ├── chat.js     # ★ v2：分页、语音、表情包、搜索、置顶/备注、撤回、火花值
│       ├── moment.js   # 动态流、发布、点赞、评论
│       ├── social.js   # 通知、红点、打招呼短语
│       ├── game.js     # ★ v3：火花值/等级、连续聊天、每日任务、签到、心动问答、随机闪聊
│       ├── group.js    # ★ v4：兴趣圈子、群聊、楼中楼评论、热榜、活跃榜
│       ├── extra.js    # ★ v5+v7：智能推荐打分、邀请裂变、虚拟礼物、VIP、充值
│       └── admin.js    # ★ v10：数据看板、用户管理、内容审核、举报处理、广播
├── public/
│   ├── index.html      # App 入口
│   ├── admin.html      # ★ 管理后台（独立页面）
│   ├── sw.js           # PWA Service Worker
│   ├── manifest.json   # PWA 清单
│   ├── css/app.css     # 深色霓虹主题
│   └── js/
│       ├── config.js   # ★ 服务器地址配置（填地址即用）
│       ├── api.js      # ★ 全部接口封装
│       ├── ui.js       # toast/modal/sheet/预览/服务器设置
│       ├── pages.js    # 登录、匹配卡片（含"为什么是 TA"）、广场
│       ├── pages2.js   # 消息、聊天（语音/表情包/搜索/礼物）、我的、资料、通知
│       ├── pages3.js   # 编辑资料、设置、相册、标签、会员、搜索
│       ├── pages4.js   # 闪聊、心动问答、任务签到、火花值、圈子、热榜、邀请、钱包
│       └── app.js      # 路由、TabBar、WebSocket、匹配弹层、礼物面板、发布/筛选
├── deploy.sh / Dockerfile / README.md
└── data/               # 运行时数据（db.json + uploads/）
```

## 三、联网机制（用户核心要求）

1. **前端可配置服务器地址**：`public/js/config.js` 的 `TIANRUO_CONFIG.server`，存 localStorage。所有请求在 `api.js` 统一拼 `(server || '') + path`，**改一个地址即可换后端**。
2. **设置入口**：App 内「我的 → 设置 → 服务器地址」；首次打开发现 `/api/health` 不通时自动弹窗。
3. **CORS 全开**，任意域名前端可直连。
4. **WebSocket 自动跟随**：`wsUrl()` 从 http/https 推导 ws/wss。
5. **一键部署**：`deploy.sh` 支持 systemd / pm2 / Docker，部署后把公网地址填进 App 即可。

## 四、版本交付时间表（基准 09:39）

| 时间 | 版本 | 主题 | 状态 |
|---|---|---|---|
| 10:39 | v1.0 | 核心闭环 | ✅ |
| 11:39 | v2.0 | 聊天深水区 | ✅（合并交付） |
| 12:39 | v3.0 | 玩法系统 | ✅ |
| 13:39 | v4.0 | 社区 | ✅ |
| 14:39 | v5.0 | 推荐与增长 | ✅ |
| 15:39 | v7.0 | 商业化与安全 | ✅ |
| 18:39 | v8.0 | 体验打磨（PWA/离线/动效） | ✅ |
| 19:39 | v10.0 | **最终版** | ✅ 提前交付（用户于 17:13 要求立即交付） |

> ⚠️ **实际执行说明**：项目中途多次被中断，且沙盒环境曾重置（node_modules 被清空）。到 16:42 时代码仍停在 v1.0，落后于时间表。
> 自 16:42 起改为**合并冲刺**：不再逐版本单独交付，而是把 v2~v10 全部功能一次性实现。
> 17:13 用户要求立即交付，最终版 v10.0 提前于 19:39 交付。
> 结果：**功能覆盖完整度 = 原计划 8 个版本的 100%**，交付节奏从"8 次分批"变为"1 次完整交付"。

## 五、v10.0 完整功能清单

### 账号
手机号验证码登录/注册、密码登录、Token 30 天、自动续期、未登录拦截、邀请码绑定

### 匹配（v1）
推荐卡片、左右滑/按钮滑、超级喜欢、每日额度、谁喜欢我、匹配成功弹层、解除匹配
**v5 增强**：智能打分推荐（距离 30% / 兴趣 25% / 年龄 15% / 问答 15% / 活跃 10% / 资料 5%）、"为什么推荐 TA"、契合度百分比

### 聊天（v1+v2+v7）
- 基础：会话列表、文字/图片、实时推送（WebSocket）、已读、输入中
- **v2**：分页加载（上滑加载更多）、语音消息（录音+播放）、表情包、消息搜索、置顶/免打扰/备注、长按菜单（复制/引用/撤回）、5 分钟撤回
- **v3**：火花值 + 连续聊天 streak（每条消息 +1，跨日 streak 计算）
- **v7**：虚拟礼物（6 款，火花币结算，提升火花值）

### 广场（v1+v4）
动态流（推荐/热门/话题）、发布九图、点赞、评论、话题榜单
**v4**：楼中楼二级评论、评论点赞、热榜（24h 加权评分）、活跃达人榜

### 玩法（v3）
- 火花值 + 6 级等级体系（小火苗→永恒之火）
- 每日任务（6 个）+ 签到（连续奖励递增）
- 心动问答（8 题）+ 契合度百分比展示
- 随机闪聊（匿名 5 分钟限时聊天，双方同意可解锁身份并转正式匹配）

### 社区（v4）
8 个兴趣圈子、圈子群聊（实时广播）、加入提示

### 商业化（v7）
火花币钱包、充值（演示直充）、虚拟礼物、VIP 三档套餐（月/季/年，火花币或演示支付）、邀请裂变（双方各得币）

### 安全（v7）
敏感词库（60+ 词）+ 变体归一化（去空格/标点/繁简/大小写）、联系方式检测、聊天与动态双端审核、举报/屏蔽、风控限流

### 管理后台（v10）
`admin.html` 独立页面：数据看板（11 项指标 + 7 日趋势图 + 性别/城市分布）、用户管理（搜索/封禁/解封）、内容审核（风险标记/删除）、举报处理（标记/封禁）、系统广播

### 系统
SVG 头像生成、图片/语音上传、在线状态、访客记录、PWA（离线外壳 + 可安装）、响应式（手机全屏 / 桌面居中 430px）

## 六、测试结果

**全链路回归：67 项通过，0 项失败**（`node /tmp/full.js`）。
覆盖 8 大模块：账号鉴权、智能匹配、聊天 v2、玩法 v3、社区 v4、商业化 v7、管理后台 v10、内容安全。

| 模块 | 项数 | 结果 |
|---|---|---|
| 账号（密码登录/验证码/鉴权拦截） | 5 | ✅ |
| 匹配（智能推荐/打分/为什么是TA/额度/滑动/谁喜欢我） | 7 | ✅ |
| 聊天（文字/语音/已读/输入中/搜索/置顶备注/表情包/撤回） | 9 | ✅ |
| 玩法（火花值/任务/签到/问答/契合度/闪聊） | 10 | ✅ |
| 社区（圈子/热榜/活跃榜/动态/评论/楼中楼/点赞） | 9 | ✅ |
| 商业化（VIP/礼物/充值/送礼物/邀请码） | 5 | ✅ |
| 用户（资料/附近/搜索/访客/举报/通知/红点） | 10 | ✅ |
| 安全（敏感词拦截/正常放行） | 3 | ✅ |
| 管理后台（看板/用户/动态/举报/鉴权/广播） | 6 | ✅ |
| 静态资源与 PWA | 8 | ✅ |

## 七、关键 Bug 修复记录

1. **store 重复加载导致读写分离（严重）**
   现象：接口返回成功但数据读不到（举报列表为空、发布的动态不显示）。
   根因：`index.js` 的 `start()` 调用 `store.load()`，而路由模块在 require 时通过 getter 已触发过一次 `load()`；第二次调用重新赋值了模块内 `db` 变量，导致已解构 `db` 的路由模块仍指向旧对象，而 `q.*` 写入的是新对象。
   修复：`load()` 改为幂等（`if (db) return db;`），新增 `reload()` 供强制重载。

2. `/api/chat/stickers` 被 `/:matchId` 路由吞掉 → 将 stickers 路由前置。
3. `extra.js` 误用 `U.uid`（utils 未导出）→ 改用 store 的 `uid`。
4. `upload.js` 相对路径写错（`../config` → `./config`）。
5. 楼中楼评论前端路径与后端挂载点不一致 → 统一为 `/api/group/moment/:id/reply`。
6. **候选池滑完即永久为空（体验致命）**：用户滑完所有推荐后 `/api/match/cards` 与 `/api/extra/recommend` 永远返回空数组，前端只能显示"附近没有更多人了"。
   修复：新增 `recycleIfStarved()`（match.js，已导出复用），当候选数 < 5 时自动清除 3 天前的 `pass` 记录，`like`/`super` 永不回收，避免刚跳过的人立刻再出现。
7. **动态与评论完全未接入内容审核**：`moderation` 只在 chat.js 引用，moment.js 的发布/评论绕过审核，与文档声称的"双端审核"不符。
   修复：发布与评论均接入审核；并把审核改为分级——辱骂/色情/违法类**硬拦截**，联系方式引流类（微信/QQ/手机号/扫码）仅**打标 + 后台复核**，不再一刀切阻断正常社交。
8. **送礼物接口参数不兼容**：前端传 `matchId`，后端只认 `toId`，导致"用户不存在"。
   修复：`/api/extra/gift/send` 同时兼容 `matchId` 与 `toId`。

## 八、已知限制

- 演示环境短信验证码固定 `1234`，未接真实短信网关（`server/routes/auth.js` 的 `sendCode` 处接入）。
- 支付为演示直充，未接真实支付渠道。
- 消息推送在 App 后台时依赖 WebSocket，未接入系统级 Push（APNs/FCM）。
- 数据为单文件 JSON，适合 10 万级用户以内；更大规模需替换为数据库。
- 语音消息依赖浏览器 MediaRecorder，iOS Safari 部分旧版本支持有限。

## 九、Android APK 封装工程（追加交付）

用户要求 APK 格式。当前环境无 Android SDK 且磁盘不足（构建需 3-5GB），无法直接编译产出 .apk，
故交付**配置完整的标准 Capacitor 6 Android 工程**，本机一条命令出 APK。

工程位置：`/data/workspace/tianruo-apk`（独立目录，自包含，不依赖主项目）

```
cd tianruo-apk && ./build-apk.sh --server http://你的IP:3000
→ android/app/build/outputs/apk/debug/app-debug.apk
```

### 已处理的原生适配（含两个关键坑）

1. **★ APK 化致命坑：Capacitor WebView 的源是 `http://localhost`**
   原 `config.js` 的 detect() 在 http/https 协议下返回空串（走相对路径），
   在 APK 里会把所有 `/api/*` 请求打到 WebView 自己身上 → 永远 404、App 完全不可用。
   修复：新增 `isNative()` 判断（`window.Capacitor.isNativePlatform()` + UA 兜底），
   原生环境下强制使用注入的 `DEFAULT_SERVER`，不走同源假设。
   该修复已同步回主项目 `public/js/config.js`，对浏览器行为无影响。

2. **★ Android 9+ 默认拦截明文 HTTP**
   自建服务器多为 `http://IP:3000`，不加配置直接连不上。
   修复：`usesCleartextTraffic="true"` + `network_security_config.xml` 放开明文。

3. 其余：权限（相机/麦克风/相册/通知，兼容 Android 13+ 的 READ_MEDIA_*）、
   竖屏锁定、深色状态栏、键盘 resize、矢量启动页、5 密度 ×3 图标（零依赖 PNG 生成器 tools/make-icon.js）、
   versionCode 10 / versionName 10.0.0、Release 自动签名。

### 构建脚本能力
环境自检（Java 17 版本提示 / Android SDK 三条获取路径引导）、前端同步、服务器地址注入（幂等）、
`cap sync`、Release keystore 自动生成 + build.gradle 签名注入、Gradle 构建、结果路径输出。
无 SDK 时输出清晰引导而非崩溃（已干跑验证）。

## 十、后续可扩展方向

地理位置围栏推荐、视频/语音通话、动态视频流、兴趣搭子匹配、AI 破冰话题推荐、实名认证与人脸核验、多语言。
