# 天弱 iOS · 云端编译已完成 ✅

## 📱 可安装的 IPA 下载地址

**GitHub Release（macOS + Xcode 15.4 真实编译产物）**：

👉 https://github.com/MRC330/tianruo/releases/tag/latest

或直接下载：`default.ipa`（7,699 字节，arm64，免签名）

### 三种下载方式（任选其一）

**方式 A：网页一键下载（最简单）**
1. 打开 https://github.com/MRC330/tianruo/actions
2. 点最新一条 `Build 天弱 IPA`（绿色 ✓）
3. 页面右上角 **Artifacts → 天弱-ipa** 下载
   （也可到 Releases 页 `latest` 下载 `default.ipa`）

**方式 B：命令行（GitHub CLI）**
```bash
gh run list -R MRC330/tianruo          # 找到最新 run id
gh run download <RUN_ID> -R MRC330/tianruo -n 天弱-ipa
```

**方式 C：curl（用你自己的 token）**
```bash
curl -L -H "Authorization: Bearer <你的TOKEN>" \
  -o 天弱.ipa \
  "https://api.github.com/repos/MRC330/tianruo/actions/artifacts/<ARTIFACT_ID>/zip"
```

---

## 🔧 云端编译链路（已打通）

| 环节 | 状态 |
|------|------|
| 代码推送到 `MRC330/tianruo` (main) | ✅ |
| `.github/workflows/build-ipa.yml` | ✅ macOS 14 + Xcode 15.4 |
| `xcodebuild archive`（真实 Mach-O 二进制）| ✅ |
| 打包 `Payload/天弱.app` → `天弱.ipa` | ✅ |
| 上传到 **Release `latest`** + **Artifact** | ✅ |

**每次你 push 代码到 main，Actions 自动重新编译出 IPA，无需手动操作。**

---

## 📲 安装到手机

`.ipa` 是**免签名**的，用侧载工具安装（免费 Apple ID 即可，7 天需重签）：

- **AltStore** (iOS) / **Sideloadly** (Win/Mac) / **爱思助手** (Win/Mac)
- 导入方式：工具内「导入 IPA」→ 选 `天弱.ipa` → 签名 → 安装

> ⚠️ 若要**上架 App Store / TestFlight** 或**免重签长期使用**，需 $99/年 Apple Developer 账号。
> 届时取消 workflow 里 `xcodebuild` 的 `CODE_SIGN_IDENTITY="-"` 相关行，改填你的 Team ID 即可自动签名。

---

## 📂 工程结构

```
tianruo/
├── .github/workflows/build-ipa.yml   # 云端编译流水线
├── ios/                              # Swift WKWebView 原生壳
│   ├── 天弱/                         # AppDelegate / ViewController / Info.plist
│   ├── 天弱.xcodeproj/project.pbxproj
│   ├── build-ipa.sh
│   └── verify_ipa.py
├── public/                           # 前端资源（自动打入 www/）
├── server/                           # Node.js 后端
├── build_ipa.py                      # 本地一键打包 + 结构校验
└── 天弱.ipa                          # 本地占位包（真包请从上方链接下载）
```

---

## ✅ 验证记录

- Actions run #38：`completed/success`，全部步骤通过
- IPA 结构：`Payload/天弱.app/{Info.plist, 天弱(可执行文件), _CodeSignature/, www/}`
- Bundle ID：`com.tianruo.app` | Version：`10.0.0` | 架构：arm64
- 后端接口冒烟：`/api/health`、`/api/auth/*` 正常

---

**下一次出包**：改代码 → `git push` → 等 3-5 分钟 → 到 Release 页下载新 IPA。
